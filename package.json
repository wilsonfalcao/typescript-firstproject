{
  "name": "typescript-example-first",
  "version": "1.0.0",
  "description": "Primeiro contato com TypeScript e projetos",
  "main": "App.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node-dev --respawn --transpile-only --ignore-watch node-mudeles --no-notify src/server.ts"
  },
  "author": "Wilson Falcao",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.5.2"
  }
}
